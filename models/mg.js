// const mongoose = require('mongoose');

// // Gantilah dengan connection string yang sesuai jika perlu
// const connectionString = 'mongodb://localhost:27017/';

// // Nama database yang ingin digunakan
// const dbName = 'myDatabase'; // Gantilah dengan nama database yang kamu inginkan

// // Menghubungkan ke MongoDB
// mongoose.connect(`${connectionString}${dbName}`, {
//     useNewUrlParser: true,
//     useUnifiedTopology: true,
// }).then(() => {
//     console.log(`Connected to MongoDB: ${dbName}`);
// }).catch((error) => {
//     console.error('Error connecting to MongoDB:', error);
// });

// module.exports = mongoose;
